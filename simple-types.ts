//primitive datatypes are boolean, string and number/ other ones are bigint and symbol

//type assignment

//explicit (specifying the type)

let myName: string = "Kennedy Anyidoho";
console.log(myName, typeof myName); // this will log : Kennedy Anyidoho string

//implicit (leaiving it to typescript to figure out the  so we say typescript infers the value)

let Name = "Kennedy Anyidoho";

console.log(Name, typeof Name); // this will log : Kennedy Anyidoho string
